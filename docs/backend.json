
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user within the Shree Label ERP system.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "roles": { "type": "array", "items": { "type": "string" } },
        "isActive": { "type": "boolean" },
        "mustChangePassword": { "type": "boolean" },
        "salesCode": { "type": "string" }
      },
      "required": ["id", "email", "firstName", "lastName", "roles", "isActive"]
    },
    "PaperStock": {
      "title": "PaperStock",
      "type": "object",
      "description": "Represents a raw substrate jumbo roll in the inventory with technical columns.",
      "properties": {
        "rollNo": { "type": "string", "description": "System-generated Unique Roll ID (RL-0001)" },
        "status": { "type": "string", "enum": ["Available", "Reserved", "Used"] },
        "paperCompany": { "type": "string" },
        "paperType": { "type": "string" },
        "widthMm": { "type": "number" },
        "lengthMeters": { "type": "number" },
        "sqm": { "type": "number", "description": "Auto-calculated: (W*L)/1000" },
        "gsm": { "type": "number" },
        "weightKg": { "type": "number" },
        "receivedDate": { "type": "string", "format": "date" },
        "purchaseRate": { "type": "number" },
        "dateOfSlit": { "type": "string", "description": "Populated by slitting module" },
        "jobNo": { "type": "string" },
        "jobName": { "type": "string" },
        "lotNo": { "type": "string", "description": "Lot No / Invoice No" },
        "remarks": { "type": "string" },
        "createdByName": { "type": "string" }
      },
      "required": ["rollNo", "paperCompany", "gsm", "widthMm", "lengthMeters"]
    },
    "PaperMetadata": {
      "title": "PaperMetadata",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "Employee profiles and access control."
    },
    "/paper_stock/{rollId}": {
      "schema": { "$ref": "#/backend/entities/PaperStock" },
      "description": "Main substrate inventory registry with sequential Roll ID."
    },
    "/adminUsers/{adminUid}": {
      "schema": { "type": "object" },
      "description": "Authorization markers for system administrators."
    },
    "/paper_companies/{id}": {
      "schema": { "$ref": "#/backend/entities/PaperMetadata" },
      "description": "Master list of paper companies."
    },
    "/paper_types/{id}": {
      "schema": { "$ref": "#/backend/entities/PaperMetadata" },
      "description": "Master list of paper types/substrates."
    },
    "/counters/{id}": {
      "schema": { "type": "object" },
      "description": "Atomic sequence counters for unique ID generation."
    }
  }
}
